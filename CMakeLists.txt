# M5Stack Core Ink Switch - CMakeLists.txt
# Simple ESP-IDF project for build/flash verification (no Matter yet)
# Target: ESP32-PICO-D4 (idf_target = esp32)

cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(M5CoreInk-Switch)
